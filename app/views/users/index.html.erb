<h1>IEOR 4998 Survey Tool Users</h1>

<table>
  <tr>
    <th>Login</th>
    <th>Name</th>
    <th>Password</th>
    <th>Imagelink</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.Login %></td>
    <td><%= user.Name %></td>
    <td><%= user.Password %></td>
    <td><%= user.ImageLink %></td>
    <td>
	<%= link_to 'Show', user %><br/>
    <%= link_to 'Edit', edit_user_path(user) %><br/>
    <%= link_to 'Destroy', user, :confirm => 'Are you sure?', :method => :delete %>
	</td>
	<td><%= image_tag(user.ImageLink, :class => 'list_image') %> </td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User', new_user_path %>
<p> <%= link_to "Log Out", welcome_goodbye_path %> </p>
